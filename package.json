{
  "name": "gp-du-monde-cargo",
  "version": "1.0.0",
  "description": "Système de gestion des cargaisons GP du Monde",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run php-server\" \"npm run json-server\" \"npm run watch-ts\"",
    "php-server": "php -S localhost:8000 -t .",
    "json-server": "json-server --watch db.json --port 3001",
    "watch-ts": "tsc --watch",
    "build": "tsc",
    "start": "npm run dev"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  },
  "dependencies": {}
}